# Dış skill denetimi — 2026-09-19 (AIOps · MCP · asistan)

Operatör isteği: 2026-09-19'da kurulan dış skill'ler (Anthropic incident-response
+ runbook, wshobson incident-runbook-templates + postmortem-writing, Grafana
oncall-irm, Elastic kibana-anomaly-detection, Dynatrace predictive analytics,
vasilyu1983 agents-mcp [MCP spec 2026-07-28] + ai-llm-inference, Anthropic
mcp-builder, Honeycomb production-investigation + slos-and-triggers +
observability-fundamentals) Coremetry'ye ne katar? Dört salt-okunur denetim
ajanı her skill'i yükleyip repoya uyguladı (v0.10.792 ağacı); repo
sözleşmelerinde (CLAUDE.md, `.claude/skills/*`, özellikle `/aiops`,
DECISIONS, INCIDENTS) zaten kabul edilmiş kararlar bulgu sayılmadı. Operatör
direktifleri korundu: P1 yapışkan, Problems başlığında şerit yok,
service_silent kapalı, redaction yok, tek kiracı, exception tabanı 2,
per-kanal NotifyKinds, "prefetch+narrate > tool-loop", RAG bloklu. Her bulgu
dosya:satır kanıtlı; uygulanmadan önce ±10 satır bağlam okunur
(`feedback-audit-verify-context`). Bölüm öneki: I incident/on-call, A
anomali, M/L MCP/LLM runtime, S/V SLO/tetik ve soruşturma akışı.

Sıralama ilkesi: incident kaybettiren / yanlış page > sahte-kaçan anomali >
güvenlik > eksik yetenek > cila. Tahminler: ~10 dk / ~30 dk / ~1 saat /
~2 saat / ~yarım gün / ~1 gün. "Tür" sütunu: **davranış değişikliği → onay**
(mevcut yüzeyin varsayılanı/kapsamı değişir; operatör onayı olmadan
uygulanmaz) ya da **additive** (yeni yetenek, mevcut davranış aynen).

## I. Incident / on-call / runbook / postmortem (skill'ler: incident-response, oncall-irm, incident-runbook-templates, postmortem-writing, runbook)

Kaynak: internal/chstore/incident*.go, problem.go, problem_escalation.go, runbooks.go, runbook_executions.go, settings.go, notification_ignore.go; internal/api/{api_incidents,runbook,runbook_exec,postmortem_draft,notify_actions,inbox}.go; internal/notify/{notify,incident_alert,ignore_link,routing,problem_dedup}.go; internal/evaluator/evaluator.go; internal/copilot/prompts.go; frontend/src/pages/{Incident,Incidents,Runbook,Inbox}.tsx.

| # | Şiddet | Skill | Coremetry bugün (kanıt) | Öneri | Tahmin | Tür |
|---|---|---|---|---|---|---|
| I1 | 🔴 | oncall-irm / incident-response: incident şiddeti alert grubuyla yaşar, SEV yeniden değerlendirilir | Incident şiddeti ilk problemden bir kez yazılır (`internal/chstore/incident.go:441`); kural 2 "same service, ignore severity" sonraki critical problemi warning incident'a bağlar, şiddeti YÜKSELTMEZ (`:379-390`). Yaş eskalasyonu problemi critical yapar (`evaluator.go:1235`) ama incident warning kalır → bildirim P2 (`problem.go:395-401`), Inbox P2 (`api/inbox.go:1533-1540`), liste warning (`Incidents.tsx:147`) | `AttachProblemToIncidentWith` (~`incident.go:420-430`): `severityRank(p) > severityRank(inc)` ise `UpsertIncident` + `IncidentEvent{Kind:"severity_raised"}`; `incidentLifecycleNotifies` (`incident_lifecycle.go:33`) bu türü bildirsin; test `incident_lifecycle_test.go`; `Incident.tsx:436-456` kindLabel | ~1 saat | Davranış değişikliği → onay **GEMİDE v0.10.802** |
| I2 | 🔴 | oncall-irm: eskalasyon zinciri notify → wait → notify tekrar → notify team → webhook, ≤4 basamak | Tek mekanizma yaş merdiveni info→warning→critical (`problem_escalation.go:23-51`, `evaluator.go:1203-1251`), her basamak AYNI kanallara; critical'dan sonra hiçbir şey olmaz. Ack kısa devresi (`notify.go:479`) + aynı-durum 1 sa tabanı (`problem_dedup.go:89,111`) → bakılmayan P1 sessiz. Ekip maili yalnız ilk açılışta (`routing.go` teamMailAlreadySent) | `problem_escalation` blobuna `UnackedRepeatSec`, `EscalateToLevel2Sec`; `ChannelMatchRules.EscalationLevel`; evaluator tikinde `repageUnacked` (P1, ack yok, son N içinde log yok → `SendProblemAlert(escalated=true)`); seviye-2'de SRE ekibine ikinci mail | ~yarım gün | Additive (varsayılan kapalı) |
| I3 | 🟡 | Investigating → Identified → Monitoring → Resolved; detected/acknowledged/mitigated/resolved damgaları | Durumlar open/acknowledged/resolved (`incident.go:21`, `types.ts:220`); mitigated yok; AckAt hiç gösterilmiyor (`Incident.tsx:222-224`), listede ack sütunu yok | Timeline türü `mitigated` + `POST /api/incidents/{id}/mitigate` (ack klonu `api_incidents.go:106-134`) + düğme; `Incident.tsx` "acknowledged +Xm / mitigated +Xm" çipleri | ~1 saat | Additive |
| I4 | 🟡 | ChatOps: mesajdan Acknowledge / Resolve / Silence | Yalnız "Sustur" bağlantısı (`ignore_link.go:36-51`, `notify_actions.go:39`); ignore 30 gün ve çözüm mailini de yutar (`notification_ignore.go:8-10`, `notify.go:486`) | Aynı imzayla `GET /api/public/notify/ack/{token}` ve `/resolve/{token}`; şablonlara "Onayla" alanı; notification_log `channel_kind='ack'` | ~2 saat | Additive |
| I5 | 🟡 | postmortem = özet · etki · zaman çizelgesi tablosu · kök neden · 5 Neden · iyi/kötü giden · MTTA/MTTR · aksiyon | AI taslağı beş bölüm (`postmortem_draft.go`, `prompts.go:1161-1198`); kanıt 40 timeline olayı taşıyor ama tablo istenmiyor; 5 Neden, iyi/kötü giden, MTTA/MTTR yok (`pmEvidenceInput:55`) | `systemPostmortem`'a Zaman çizelgesi, 5 Neden, Tespit ve müdahale (MTTA/MTTR kanıt kurucuda), İyi/kötü giden; golden test | ~30 dk | Prompt değişikliği → onay (risk düşük) |
| I6 | 🟡 | SEV1/SEV2 → postmortem zorunlu; aksiyonlar sahipli/tarihli, takip edilir | Postmortem serbest metin (`incident.go:25`); listede "postmortem eksik" yok; aksiyonlar `- [ ]` satırı sayılmıyor | Incidents "PM" sütunu ✓/—/gerekli + `needs_postmortem` süzgeci; postmortem kartında `- [ ]`/`- [x]` sayımı "aksiyon 2/4" | ~2 saat | Additive |
| I7 | 🟡 | runbook adımı: Expected + If it fails, Prerequisites, Verification, Rollback, Escalation, Owner, Last verified | `RunbookStep` (`runbooks.go:53-64`) OnFailure/rollback yok; `Runbook` (`:22-42`) Owner/LastVerified/Escalation yok; UI serbest markdown'a yönlendiriyor (`Runbook.tsx:428`) | `RunbookStep.OnFailure/Rollback` (steps_json, DDL'siz); runbook düzeyi `meta_json` (Owner, LastVerifiedAt, Escalation[]) ALTER ADD iki-boot; "son doğrulama > 90 g" rozeti | ~yarım gün | Additive |
| I8 | 🟡 | runbook incident'tan koşulur; History kim/ne zaman | Koşular yalnız ProblemID (`runbook_executions.go:22`); Incident sayfasında runbook paneli yok (`Incident.tsx:307-330`); `ProblemRunbookPanel` yalnız problem çekmecesinde | `IncidentRunbookPanel`: IncidentProblems id'leri için `ListExecutions{ProblemID}` (≤20) + "Run runbook" | ~1 saat | Additive |
| I9 | 🟡 | schedule/rotation, "şu an kim nöbetçi", ekip bildirimi nöbetçiye | Nöbet varlığı yok; `ServiceMetadata.OncallURL` yalnız link (`service_metadata.go:46`), hiçbir bildirime girmiyor; ekip maili statik liste (`settings.go:516-541`) | (a) şablonlara "On-call ↗" alanı (`notify.go:1595` emsali) ~30 dk; (b) `TeamContacts.OncallICalURL` → `OncallEmailsForTeam(now)` iCal'den, 5 dk cache, boşsa statik ~yarım gün | ~30 dk / ~yarım gün | Additive **GEMİDE v0.10.798 (yalnız a: bildirimde On-call bağlantısı)** |
| I10 | 🟡 | her 15 dk durum güncellemesi (durum·etki·yapılan·sonraki) | Notlar bilerek sessiz (`incident_lifecycle.go:33-36`, v0.10.748); "son güncelleme" göstergesi yok; status page güncelleme günlüğü yok (`statuspage.go:43-49`) | Yeni eylem "Durum güncellemesi" → `IncidentEvent{Kind:"status_update"}` bildirilir; `Incident.tsx` "son güncelleme Xm" çipi, `StatusUpdateCadenceMin` aşılınca uyarı | ~2 saat | Additive |
| I11 | 🟢 | roller: IC, comms, responders | Tek Assignee, ack'leyene otomatik (`api_incidents.go:118-122`) | `IncidentEvent{Kind:"role"}` + `POST /api/incidents/{id}/role` → çipler | ~2 saat | Additive |
| I12 | 🟢 | tek görünür şiddet ölçeği | Eşleme tutarlı (`problem.go:395-401`, `inbox.go:1533-1551`) ama Incidents listesi/detayı P rozeti çizmiyor (`Incidents.tsx:147`, `Incident.tsx:196-197`) | `chstore.IncidentPriority(inc)` saf; zarfa priority/priorityReason; `PriorityBadge` listede + detayda | ~30 dk | Additive **GEMİDE v0.10.796** |
| I13 | 🟢 | yönlendirme Preview: "bu alarm hangi zincire gider" | `POST /api/channels/{id}/test` var (`api.go:1286`); "bu problem hangi kanallara gider" yok | `POST /api/notify/preview {service,severity,kind,priority}` → kanal listesi + neden; ChannelModal ve problem çekmecesi | ~1 saat | Additive |
| I14 | 🟢 | silence geçici, çözüm yine gelir | "Sustur" sabit 30 gün, çözüm dahil keser (`notification_ignore.go:39-49`); jeton TTL taşıyor ama mağaza okumuyor (`ignore_link.go:27`) | Süre seçimi (1 sa / 24 sa / 30 g), `until`; çözüm bildirimi her hâlde | ~1 saat | "Sustur" semantiği değişir → onay |
| I15 | 🟢 | etki büyüdükçe başlık/etki güncellenir | Başlık ilk problemden donuk (`incident.go:433-437`); bağlı/açık problem sayısı listede yok; `OpenIncidentRollups` (`incident.go:531`) zaten sayıyor | `listIncidents` zarfına problemCount/unresolved; "Problems 3 (2 açık)" sütunu | ~30 dk | Additive **GEMİDE v0.10.797** |

Zaten iyi: auto-resolve + çözüm bildirimi + incident kaskadı (`evaluator.go:638-724`, `notify.go:471-479`); 30 dk aynı servis + 1-hop komşu gruplama (`incident.go:369-420`); AND'li routing predikatları (`settings.go:113-139`); koşu takibi 3 katman audit (`runbook_executions.go:11-45`); postmortem taslağı + KB indeksleme + koşudan runbook güncelleme önerisi; maintenance window.

## A. Anomali tespiti (skill'ler: kibana-anomaly-detection, dt-obs-predictive-analytics)

Kaynak: internal/anomaly/{anomaly,verdict,behavior,clustering,db_capacity}.go, internal/correlator/{hypothesis,fusion,rootcause_worker,investigation,temporal}.go, internal/chstore/{anomaly_sensitivity,anomaly_promotion,anomaly_verdict,anomaly_event}.go, internal/api/inbox.go, frontend ProblemDetail/ProblemsSection. Bilinen açık soru (bulgu değil): anomali dedektörünün error_rate'i hâlâ service_summary_5m (tüm span) — evaluator 783'te giriş span'ine geçti (`anomaly.go:1029-1033`, `:1133-1150`).

| # | Şiddet | Skill | Coremetry bugün (kanıt) | Öneri | Tahmin | Tür |
|---|---|---|---|---|---|---|
| A1 | 🔴 hafta sonu sahte anomali | Kibana: haftalık mevsimsellik ≥3 hafta/≥2 tam döngü; DT seasonal 7 g gün-saat profili | `seasonalDays=14` (`anomaly.go:72`, `anomaly_promotion.go:96`) + `seasonalMinDays=3` (`:1377`) + sınıflar weekday/saturday/sunday (`:1215-1227`). Cumartesi sınıfı 14 günde en fazla 2 farklı Cumartesi (`upper = at−(radius+bucket)`, `:1338`) → `pruneSeasonalByDayDiversity` (`:1382-1390`) seriyi düşürür → `chooseBaseline` (`:1393-1398`) düz 24 sa pencereye iner: her Cumartesi/Pazar mevsimsel taban fiilen KAPALI (v0.8.250'nin kapattığı diurnal FP hafta sonu geri geldi). Test yok (`baseline_hygiene_test.go:11` saf işlev; `anomaly_seasonal_test.go` hafta sonu × 14 g kapsamıyor) | `seasonalMinDaysFor(class)`: weekday 3, saturday/sunday 2 — ya da hafta sonu sınıflarında lookback 21-28 g; `anomaly_seasonal_test.go` "14 g + Cumartesi → mevsimsel taban" satırı | ~30 dk + test | Davranış değişikliği (hafta sonu daha az/farklı Problem) → onay **GEMİDE v0.10.799** |
| A2 | 🟡 kaçan bileşik sinyal | Kibana: 5 job × skor 30 tek yüksek skoru yener; DT: önce novelty | `openZ=3.5` tanımlı (`anomaly.go:40`) ama `decideAnomaly` (`:322-330`) severity≠critical ise BOŞ; 3.5σ–6σ bandı ne `anomaly_events`'e ne kümelemeye (`clustering.go:57`) ne hipotez Signals'a (`fusion.go:198-204`) girer | `verdict.go:34` `evaluateAnomaly`: `openZ ≤ |z| < criticalZ` → `Action="observe"`; `scan()` bunları `anomaly_events` kind=`metric_deviation` olarak toplu yazar (Problem YOK, bildirim YOK); `evaluator.go:1265` terfi süpürmesine kind kapısı + test | ~yarım gün | Additive (P1-only kapısı aynen) |
| A3 | 🟡 RCA: hangi varlık? | Kibana influencer_score; "hangi instance sürdü" | Metrik dedektörü servis düzeyi (`buildAllBucketsQuery` GROUP BY service_name); `hypothesis.go:300-470` `Synthesize` katmanları deploy/komşu/aynı-servis/eş-ateşleme — servis İÇİ kırılım (op/pod/kanal) yok; derin kanıt (`investigation.go:72`) yalnız LLM prompt'una, skora değil; `ProblemOffenders` yalnız UI | Yeni saf tier `Influencers []{Dim, Value, Share}`; `fusion.go:98` tik başına tek toplu `operation_summary_5m` okuması (cari − baseline farkının operasyon payı); pay ≥ %30 ilk 3 → aday Kind="influencer"; `hypothesis_influencer_test.go`; `problemInsight.ts` "sürükleyen" hücresi | ~yarım gün | Additive |
| A4 | 🟡 desen sınıfı | DT novelty: SPIKE / CHANGE_IN_VALUES (adım) / TREND / VARIABILITY | Yön yalnız spiked/dropped (`anomaly.go:307-311`); `behavior.go:441-458` regime (adım) + seasonal var; eğim/trend ve varyans değişimi yok; regresyon çekirdeği (`db_capacity.go:69`) yalnız DB kapasitesinde | (a) `behavior.go` üçüncü sinyal `trend` (3 sa OLS/Theil-Sen, R² ≥ 0.6); (b) `verdict.go` `MAD(son 12)/MAD(baseline) ≥ 3` → `anomaly_events` kind=`variability`; (c) açıklamaya `pattern: spike|drop|step|trend|variability` | (a) ~yarım gün · (b) ~2 sa · (c) ~30 dk | Additive |
| A5 | 🟡 tutarsız sıralama | Kibana tek 0-100 normalize skor, bantlar 75/50/25 | Açılma kapısı z (`|z| ≥ 6`, `anomaly.go:316`); öncelik oran (`problem.go:415-436` Value/Threshold ≥ 2 → P1, Threshold = medyan); inbox olayları üçüncü merdiven (`inbox.go:2084-2098` peak ≥5× P1); dördüncü ölçek `behaviorScore` (`behavior.go:480`). Örnek: %1→%1.9 @12σ = P2; 100→210 ms @6.1σ = P1 | `internal/anomaly/score.go` saf `anomalyScore(z, ratio, runLen) → 0..100`; açıklamaya "score 82 — 8.1σ · 2.4× · 45 dk"; `anomalyPriority`/behaviorScore aynı bantlara; öncelik merdiveni DEĞİŞMEZ | ~yarım gün | Skor: additive · inbox bant değişimi: onay |
| A6 | 🟡 soğuk başlangıç | Kibana: yetersiz eğitim → orta sapma aşırı; DT violatingSamples | `MinBaselineRate` üç metrikte 0 (`anomaly_sensitivity.go:355-380` → `hasEnoughVolume` herkesi geçirir); `enoughHistory` 75 dk (`anomaly.go:1050`); düz pencereye düşüş SESSİZ (`verdict.go:16-32` taban türü taşımıyor) | (1) `anomalyOutcome.BaselineKind/BaselineN` + açıklama "vs 14 g aynı-slot (n=41)" / "vs son 24 sa (n=12, cold start)"; (2) flat + geçmiş < seasonalMinDays → Problem yerine A2 olayı (ya da criticalZ+2); (3) `MinBaselineRate` sıfır-olmayan varsayılan (ör. 0.2 rps); `sensitivity_test.go:327` | (1) ~1 sa · (2)+(3) ~2 sa | (1) additive · (2)(3) onay |
| A7 | 🟡 süre/tepe | Kibana multi_bucket_impact, anomaly_length skoru artırır | `evalWindow` ikili 3/3 dwell (`anomaly.go:354-381`); "sustained %d buckets" sabit dwell metni (`:758-759`), gerçek koşu değil; `open.Value = current` (`:767`) → tepe kaybolur (anomaly_events peak taşır, Problem taşımaz) | `applyOutcome`: `runLen = (now − StartedAt)/5 dk` → "sustained 47 dk (9 kova), peak 3.4× @14:35"; tepe = max(open.Value, current) metinde; `anomaly_dwell_test.go` | ~1 sa | Additive |
| A8 | 🟡 bakım penceresi | Kibana: takvim olayıyla bastır; DT: önce önceki gün/haftayla kıyasla | Bakım penceresi YALNIZ bildirimde (`notify.go:463-470`, `evaluator.go:961-966`); `anomaly.go:481-697` `scan()` `ListMaintenanceWindows` okumaz → Problem açılır, incident'a bağlanır; taban sorguları pencereyi dışlamaz → 03:00 bakımı 14 günün 03:00 slotunu kirletir | `scan()` tik başına pencereleri yükle, `inMaintenance(svc, now)` → `Action="skip"`; tabanda `excludeWindows(series, ts, windows)`; `anomaly_maintenance_test.go` | ~2-3 sa | Açılışı atlama: onay · tabandan dışlama: additive |
| A9 | 🟡 kalibrasyon | Kibana explain/custom_rules; DT significanceLevel — dedektör ölçülür | Operatör verdict'leri (`anomaly_verdicts`) yalnız okuma zenginleştirmesi (`anomaly_verdict.go:93`); replay/backtest yok; dedektör testleri sentetik | `internal/anomaly/backtest_test.go` (`//go:build backtest`): N günlük service_summary_5m fixture → tik tik `evaluateAnomaly` replay → açılış/gün, medyan süre, verdict/silence uyumu; sonra `/api/admin/anomaly/calibration` | ~yarım gün | Additive |
| A10 | 🟢 açıklanabilirlik | Observed / Expected / Deviation / Type / Action | σ/baseline/yön/dwell yalnız Description'da (`anomaly.go:758-759`); `ProblemDetail.tsx:1080` anomali probleminde description GİZLİ; "/ threshold" etiketi aslında medyan | `ProblemDetail.tsx` anomali için yapılandırılmış bölüm (Gözlenen/Beklenen/Sapma/Desen/Süre; `parseAnomalyDesc` saf); "/ baseline" etiketi | ~1-2 sa | Additive (FE) |
| A11 | 🟢 skor sürüklenmesi | initial_record_score vs record_score birlikte | Her tazelemede `open.Value = current` (`anomaly.go:767`); öncelik okuma anı (`problem.go:368`) → açık P1 anomali soğudukça P2'ye iner, inbox sırası kayar; ilk skor saklanmıyor | Tepe ile: `computePriority` oranı `max(current, peak)/median` (onay) — onaysız yol tepe yalnız metinde | ~1 sa | Öncelik: onay · metin: additive |
| A12 | 🟢 kapasite | DT days-to-saturation | `capacityETA` (`db_capacity.go:69-121`) yalnız DB; limitli gauge'larda (PVC, container memory) yok; JVM heap kuralı emekli (v0.9.551) | `capacityETA` → `evaluator/forecast.go`; opt-in `capacity:<gauge>` kontrolü, "projected full in ~Nh" | ~yarım gün | Additive (opt-in) |

Zaten iyi: robust medyan+MAD (`anomaly.go:1404`), yön-farkındalı politika, asimetrik aç/çöz, mevsimsel slot ±15 dk + sat/paz ayrımı, sıfır-dolgu kırpma, iki-fazlı kümeleme, zamansal Spearman gölge kipi, deploy etkisi ölçümlü gerekçe. aiops kontrol listesi: A2/A4 yeni anomaly_events kind → FingerprintAnomaly + mergeAnomalyCarry + terfi kapısı testi; A3 → hypothesis testleri + rca_calibration; A5/A11 → computePriority saf, gerekçe cümlesi doğru.

## M/L. MCP sunucu + istemci ve yerel-LLM copilot (skill'ler: agents-mcp v1.5 [spec 2026-07-28], mcp-builder, ai-llm-inference v1.3)

Kaynak: internal/mcp/{mcp,observe,toolerr,cancel}.go, internal/mcpclient/{mcpclient,registry,settings,transport_http,transport_stdio}.go, internal/mcptools/tools.go, internal/api/{mcp_gate,mcp_observe,mcp_client_routes,chat_mcp_bridge,copilot_chat,copilot_guided,chat_tool_budget}.go, internal/copilot/{copilot,stream,provider_calls,profiles,modelcaps}.go, internal/copilot/provider/{openai,stream}.go, internal/anomaly/{problem_explainer,investigation}.go, charts/coremetry/values.yaml, docs/runbooks/mcp-claude-code.md.

**Doğrudan cevaplar:** sunucu iki sürüm ilan eder — `ProtocolVersion = "2024-11-05"` (SSE, `mcp.go:86`) ve `ProtocolVersionStreamable = "2025-03-26"` (`POST /api/mcp`, `mcp.go:92`); istemci `"2024-11-05"` gönderir (`mcpclient.go:34`), `MCP-Protocol-Version` başlığı yok (`transport_http.go:97-110`). Sunucu initialize'ı ZORLAMIYOR: Streamable yolda `dispatch(ctx, nil, req)` initialize'sız çalışır (`mcp.go:876-881`), `notifications/initialized` 202 (`:956-960`), `Mcp-Session-Id` üretilmez (`:818-825`) → fiilen stateless; eksik olan 2026-07-28'in `server/discover` + `_meta` sözleşmesi (`server/discover` → -32601, `mcp.go:939-941`). Auth: çerez-JWT ya da `cmk_` bearer + rol/tool `MinRole` + 60/dk (`mcp_gate.go:35-36,108-137`); OAuth yok (air-gapped'de kabul). Tool sınırları: `clampLimit` (`tools.go:560`), `range_s` ≤ 7 g, `has_more`, 20 s bütçe (`toolerr.go:203`), 4 MB gövde, sohbet tool sonucu ≤ 6000 rune. Hata: uygulama hatası JSON-RPC başarı + `isError` + `{error,retryable,hint}` (`toolerr.go:111-115`); protokol −32700..−32603, −32000 rate, −32001 forbidden. LLM: prefetch bağlam önbelleği yalnız ad listeleri 60 s Redis (`copilot_guided.go:1852-1927`); explain/guided SSE delta + 15 s heartbeat, serbest tool döngüsü buffered (bilinçli); timeout 180 s tek `http.Client` (`copilot.go:416`), profil 10–600 s, tool 20 s, MCP istemci 15 s/60 s; retry: 5xx/timeout'ta yok, stream→buffered bir kez, JSON kipi merdiveni 400/422/501'de, 429/kota 1 sa devre kesici (`copilot.go:736-741`).

| # | Önem | Skill | Coremetry bugün (kanıt) | Öneri | Tahmin | Tür |
|---|---|---|---|---|---|---|
| M1 | 🔴 uyumsuzluk | agents-mcp 2026-07-28: initialize kalktı, her istek `_meta`'da sürüm+yetenek, zorunlu `server/discover` | 2024-11-05 (SSE) + 2025-03-26 (Streamable) ilan (`mcp.go:86,92`); `server/discover` −32601 (`:939-941`); `_meta` okunmuyor. Stateless taşıma zaten var | `dispatch()`'e `server/discover` → `{protocolVersions:["2026-07-28","2025-03-26","2024-11-05"], serverInfo, capabilities}`; `Request.Params._meta.protocolVersion` oku/logla; initialize legacy kalır; `mcp_test.go` discover + `_meta` | ~0.5–1 g | Additive |
| M2 | 🔴 uyumsuzluk | 2026-07-28 sunucuları initialize'ı tanımaz; istemci `_meta` taşımalı; 2025-06-18+ HTTP sunucuları `MCP-Protocol-Version` başlığı bekler | `Client.Initialize` zorunlu ön koşul (`mcpclient.go:193,232`); initialize −32601 dönerse ListTools/CallTool kırılır; sürüm "2024-11-05"; başlık yok. Bugün dış sunucu yok → gizli risk | `Initialize`: önce `server/discover`; başarılıysa stateless kip (initialize atla, her istek `params._meta`); −32601 ise legacy; `httpTransport.post` müzakere edilen sürümü başlıkta; `mcpclient_test.go` iki yol | ~1 g | Additive |
| M3 | 🔴 güvenlik | stdio için scoped env; sırlar süreç bağlamına sızmasın | `newStdioTransport` `cmd.Env` ayarlamıyor (`transport_stdio.go:62`) → dış stdio sunucusu Coremetry'nin TÜM ortamını (AI key, CH/ES kimlik, JWT sırrı) miras alır; `ServerConfig.Env` yok (`mcpclient.go:53-69`) | `ServerConfig.Env` + `cmd.Env = {PATH,HOME,LANG,TZ} ∪ cfg.Env` allowlist, `cmd.Dir` sabit; snapshot sırsız; test "çocuk süreç COREMETRY_* görmez" | ~0.5 g | Davranış değişir (miras env'e güvenen stdio sunucusu kırılır; bugün yok) **GEMİDE v0.10.803** |
| M4 | 🔴 güvenlik | HTTP'ye açık MCP'de Host/Origin doğrula (DNS rebinding) | `cors()` her `Origin`'i yansıtıp `Allow-Credentials: true` (`api.go:11446-11456`), `/api/mcp*` içinde (`api.go:1382`); çerez-JWT yolu için çapraz-origin `tools/call` yüzeyi (SameSite'a bağlı, doğrulanmadı) | `COREMETRY_ALLOWED_ORIGINS` allowlist; eşleşmeyen origin'de header yazma; `withMCPRequest`'te allowlist dışı Origin → 403; `Vary: Origin` | ~0.5 g | Davranış değişir (allowlist dışı tarayıcı origin'leri kırılır — amaç bu) **GEMİDE v0.10.804** |
| M5 | 🟡 | Tool annotation hints (readOnly/destructive/idempotent/openWorld); outputSchema | `toolListEntry` yalnız name/description/inputSchema (`mcp.go:1012-1016`); 56 tool salt-okunur ama istemci onay diyalogları bilemiyor | `mcp.Tool.Annotations`; `handleToolsList` annotations yazar (tek varsayılan); yapısal test; outputSchema ikinci adım | ~0.5 g / ~2 g | Additive |
| M6 | 🟡 | vetted tool tanımlarını hash'le pinle; değişince yeniden doğrula (tool shadowing) | Registry TTL 5 dk + `tools/list_changed` otomatik yeniler (`registry.go:28,205-220`); AllowTools/DenyTools var ama varsayılan boş; dış açıklamalar sanitizasyonsuz prompt'a (`chat_mcp_bridge.go:105-109`) | Test/Save anında `(name,description,schema)` sha256 → Settings; `refreshLocked` farkta `stale+drift`, AllowTools içermeyen yeni/değişen tool düşer + audit; UI "katalog değişti — yeniden onayla" | ~1–2 g | Davranış değişir (katalog değişince yeni tool onaya kadar görünmez) |
| M7 | 🟡 | 401'de `WWW-Authenticate`; auth-failure kuralı: bir kez yeniden kimlik, bir kez tekrar, sonra taşıma+sunucu+hata ile dur | Sunucu 401 çıplak (`auth.go:562`); istemci 401/403 → anında hata, tekrar yok, mesajda taşıma/sunucu yok (`transport_http.go:125-127`) | MCP yolunda `WWW-Authenticate: Bearer realm="coremetry-mcp"`; runbook "auth = API key, OAuth bilinçli yok"; `Registry.Call` 401'de Settings'i bir kez yeniden oku + bir tekrar; hata metni `"%s taşıması, %s sunucusu: http %d"` | ~0.5 g | Additive |
| M8 | 🟡 | HTTP+SSE 2026-07-28'de Deprecated (≥12 ay) | `/api/mcp/sse` + `/messages` pod-lokal session (`mcp.go:406-420`); chart `sessionAffinity: ClientIP` yalnız bunun için (`values.yaml:146-161`) | `HandleSSE` girişinde deprecated logu/sayacı; runbook'a kaldırma tarihi; helm skill notu "affinity yalnız SSE" | ~0.25 g | Additive |
| M9 | 🟢 | deterministik tool listesi; sağlık kapısı = list → config → düşük maliyetli çağrı; belge doğruluğu | `handleToolsList` map sırası (`mcp.go:1018-1042`); `Settings.Test` yalnız initialize+tools/list (`settings.go:220-236`); `mcp.go:56-59` ve `mcp-tools/SKILL.md:8-9` bayat ("2024-11-05 over HTTP+SSE") | `sort` ada göre; `Test`'e opsiyonel probeTool; başlık yorumu + skill güncelle | ~0.25 g | Additive **GEMİDE v0.10.795** |
| L1 | 🟡 | kuyruk derinliği/eşzamanlılık API sınırında; etkileşimli işler batch'i geçsin; overload sebebi loglansın | Tek GPU'yu arka plan explainer (16/tick, `problem_explainer.go:44`) ile sohbet paylaşıyor; `copilot.Service`'te eşzamanlılık kapısı yok; devre kesici yalnız 429/"quota" metninde (`copilot.go:736-741`) — 503 "no slot" geçici hata sayılıp 16 kez denenir | Ağırlıklı semaphore: interactive N=2, background N=1, interactive uçuştayken background bekler (`CallMeta.Surface`); `isQuotaErr`'a 503+Retry-After kısa geri çekilme; `ai_calls.overload_reason` | ~1 g | Davranış değişir (sohbet sırasında arka plan yavaşlar — istenen) |
| L2 | 🟡 | büyük sabit system prompt → prefix caching; sabit önek önce; modeli bellekte tut | Prompt: sabit + değişken + değişken + (değişken sarmal sabit) sırası (`copilot_chat.go:468-470`) → önek önbelleği ilk bloktan sonra kırılır; katalog 5.7 KB her turda; `cache_prompt`/`keep_alive` yok (`provider/openai.go:72-89`); 180 s timeout gerekçesi soğuk yükleme; `cached_tokens` kaydedilmiyor | Sabit → katalog (sabit sıra) → değişken sırası; `Request.ExtraBody` (v0.10.534) ile Ollama `keep_alive:-1`, llama.cpp `cache_prompt:true`, vLLM `--enable-prefix-caching` reçetesi; `cached_tokens` → `ai_calls` | ~1 g | Additive (metin aynı, sıra değişir → evalset) |
| L3 | 🟡 | boşa token/aynı okumayı iki kez satın alma | Guided yolu kanıt paketini her soruda CH'den; tekrar muhafızı yalnız tool döngüsü içi | `runGuidedRoute` kanıt paketi `s.cache` 30–60 s + singleflight; anlatım için `sha256(system+user)` 60 s cevap önbelleği (`ai_calls` `cache_hit=true`) | ~1 g | Additive ("önbellekten" rozeti) |
| L4 | 🟢 | token-token akış, proxy buffering kapalı, kısmi cevap kurtarma | Explain/guided iyi; serbest döngü buffered (bilinçli) | Döngünün SON turu için `StreamText`; SSE'ye `X-Accel-Buffering: no` (doğrulanmadı) + ingress `proxy_buffering off` notu | ~0.5 g | Additive |
| L5 | 🟢 | TTFT/ITL ayrı ölç ve sınırla | Tek 180 s timeout hem bağlantı hem gövde; ilk bayt için tavan yok (`copilot/stream.go:28-33`) | `firstByteTimeout` profil ayarı (45 s, 0=kapalı); `ai_calls.ttft_ms`; buffered geri düşüşte kalan bütçe ctx deadline'ından | ~0.5 g | Ayar açıkken davranış değişir |
| L6 | 🟢 operatör | kuantizasyon: Türkçe ve yapılandırılmış çıktı ilk bozulan; tek kaldıraç + eval kapısı | gemma4-26b-a4b-it MoE (`investigation.go:18-20`); JSON şema merdiveni; Türkçe zorunlu; evalset var; schema-valid oranı ölçülmüyor | Runbook "gemma4 serving": vLLM FP8/AWQ W4A16, prefix caching, `--max-model-len` ~16–24k, `--max-num-seqs 4–8`, xgrammar; llama.cpp/Ollama Q5_K_M/Q6_K, keep_alive/mlock; `ai_calls.json_valid` + `cached_tokens` | ~0.25 g dok + ~0.5 g metrik | Additive |

Uyumlu (bulgu değil): hata zarfı + JSON-RPC kodları; sunucu-tarafı sınırlar; audit + span her çağrı; kimlik başına rate limit; sunucu sampling/roots/logging/elicitation kullanmıyor (deprecation saati ilgilendirmez); istemci `server__tool` öneki, 1 MiB/200 tool tavanı; tool adlandırma iki görünümlü (kompakt TR + tam EN); yüzey→profil yönlendirmesi; prefetch+anlatım kararı skill'in "küçük model → tek yapılandırılmış çağrı" duruşuyla tutarlı. Doğrulanmayan: M4 SameSite; L4 SSE başlıkları; 2026-07-28 ayrıntıları skill metninden, canlı spec'e karşı değil.

## S/V. SLO · tetik · soruşturma akışı (skill'ler: slos-and-triggers, production-investigation, observability-fundamentals — Honeycomb)

Kaynak: internal/evaluator/{slo_burn,evaluator,selfhealth_volume}.go, internal/chstore/{slo,slo_latency_mv,slo_autocreate,problem,bubbleup,rootcause_hypothesis,evaluator_reads}.go, internal/api/{copilot_guided,api,rootcause,rca_shields,api_incidents}.go, internal/anomaly/{fusion,problem_explainer}.go, internal/copilot/prompts.go, frontend Slos.tsx / BubbleUpPanel.tsx / ServiceLatencyHeatmap.tsx / alerts/ConditionPreview.tsx.

Skill özü: SLI = olay başına boolean `IF(qualifier, success)`; kullanıcıya yakın ölç; önce baseline, %99'dan başla; en az iki alarm (tükenme ~4 sa → pager, 24 sa trend → chat) + 1 sa burn >10x → pager; sayım tabanlı tetik > yüzdelik; 5-10 dk minimum süre; ad = ne, açıklama = ne yapmalı + runbook; bütçe <%10 deploy dondur, negatif hemen soruştur; %0/%100 sabit compliance = yanlış SLI; soruşturma: oryantasyon (SLO+tetik) → karakterize → BubbleUp (atlanmaz) → trace şelalesi → with/without doğrulama → bulguyu kaydet.

| # | Şiddet | Skill | Coremetry bugün (kanıt) | Öneri | Tahmin | Tür |
|---|---|---|---|---|---|---|
| S1 | 🔴 yanlış page | Yavaş burn (24-72 sa trend) → chat; yalnız 0-4 sa tükenme ve >10x → pager; her pager alarmı anında aksiyon | `slo_burn.go:39-41` warning politikası (6 sa >6 ∧ 24 sa >3) `slo:<id>:warning` açar; `evaluator.go:1203-1251 escalateStaleProblems` TÜM açık problemleri yaşlandırır (muafiyet yalnız `self-volume-spike`, `selfhealth_volume.go:116`) → yavaş burn saatlerce açık → 30 dk'da warning→critical + yeniden bildirim, 4 sa sonra staleCrit → P1 (`problem.go:368-522`). Trend alarmı her zaman pager'a | `escalationExempt`'e `strings.HasPrefix(ruleID,"slo:")` (şiddet politikadan gelir, yaşla artmaz); süpürme + tazeleme dalı aynı kelepçe (aiops §5); pin test | ~30 dk | Davranış değişikliği → onay **GEMİDE v0.10.800** |
| S2 | 🔴 yanıltıcı | Durum "Normal / Triggered / No Events"; %0/%100 sabit compliance → yanlış SLI kontrolü | `chstore/slo.go:195-198` `total==0` → SLI 1.0, Healthy=true, bütçe %100; `Slos.tsx:127-133` "100.000%" yeşil; Service çipi `healthy ?? true`; evaluator `minSpans=50` (`slo_burn.go:82-83`) sessizce atlar. Yanlış operasyon adı = kalıcı yeşil; hiçbir yüzey Total göstermiyor | `SLOStatus.NoData` + Total; `ComputeSLOStatus` total==0 → Healthy=false, NoData=true; `Slos.tsx` "Olay yok" gri rozet; `Bad==0 && Total>N` → "SLI hiç başarısız olmuyor — tanımı kontrol et"; `Bad==Total` → "ters mantık?" | ~1-2 sa | Davranış değişikliği (çip yeşil→gri) → onay **GEMİDE v0.10.801** |
| V1 | 🟡 | BubbleUp en değerli adım, sebep bariz görünse de atlanmaz | Motor var (`chstore/bubbleup.go:86`, `/api/spans/bubbleup`, Explore + ServiceLatencyHeatmap BubbleUpPanel, RootCause paketi `rootcause.go:217-243`, hakem `rca/extras.go:103-121`). Eksik: CoSRE kök-neden prefetch'i `copilot_guided.go:2102-2186 guidedRootCauseBundle` bubbleup adımı taşımıyor; `fusion.go:41-55 EvidenceBundle` / `rootcause_hypothesis.go:47-64 DeepEvidence` attribute ayrışması taşımıyor → `problem_explainer.go:240` hiç görmüyor; MCP'de bubbleup aracı yok | (1) `guidedRootCauseBundle`'a `bubbleup` adımı (rootcause.go seçim mantığı ortak yardımcı; ilk 3 attribute TR metin); (2) `DeepEvidence.BubbleUp` — tek yazıcı sentezleyici → ProblemExplainer + mail otomatik; (3) `/mcp-tools` `bubble_up` aracı. Küçük LLM için prefetch adımı, tool değil | ~yarım gün | Additive |
| S3 | 🟡 | En az iki alarm: tükenme ~4 sa → pager, 24 sa trend → chat; ayrıca 1 sa >10x | `slo_burn.go:35-42` yalnız iki burn-rate çifti; bütçe-kalan farkındalıklı tükenme alarmı yok; `ComputeSLOForecast` (`slo.go:383-424`, HoursToExhaust) yalnız UI ForecastChip + explain-slo. Bütçesi %3 kalmış SLO 5x yanarken alarm yok; %95 bütçeli taze SLO 14.4x'te hemen page | Üçüncü politika: tik başına `ComputeSLOForecast(slo, 1h)`; `!SafeBurn && HoursToExhaust<=4` → `slo:<id>:exhaustion` critical, `<=72` → warning; Description'a saat; yeni kural id → aiops §2 tüketici listesi + pin | ~2-3 sa | Additive (yeni kural id, bildirim hacmi artabilir) |
| S4 | 🟡 | Ad = ne; açıklama = runbook + ilk müdahale; bütçe <%10 dondur, negatif soruştur | `slo_burn.go:102-109` Description "exhausted in days" — sayı/bütçe/öneri yok; `slo:` id alert_rules'ta yok → yalnız servis kataloğu runbook'u (`problem.go:660-696`) | `evaluateSLOBurn` içinde status+forecast bir kez (S3 ile ortak) → "Bütçe kalan %X · tükenme ~Y sa · öneri: deploy dondur / on-call"; `SLO.RunbookURL` ya da `EnrichProblemsWithRunbooks` `slo:` dalı | ~2 sa | Additive (metin) |
| V2 | 🟡 | Doğrulama: şüpheli sebep İLE ve ONSUZ sorgu; tek trace anekdottur | `BubbleUpPanel.tsx:37,163-170` yalnız `=` süzgeci (WITH), hariç tutma yok; `guidedWindowCompareBundle` (`copilot_guided.go:3767`) zaman kıyaslar, attribute değil; `rca_shields.go` güveni yalnız düşürür, veriye dayalı karşı-olgu yok | (a) BubbleUp değer çipine "≠ hariç tut"; (b) `/api/spans/verify?filter=…` tek taramada `countIf` ile içi/dışı RED; (c) guided adım `verify_hypothesis` ("ile: hata %X p99 Y · onsuz: …") | ~yarım gün | Additive |
| S5 | 🟡 | Sayım tabanlı tetik (>N istek 2 sn'den yavaş / 5 dk) P99'dan iyi | Builtin'ler error_rate % ×3, P99 ms ×6 (`evaluator.go:286-362`); motor kataloğunda "yavaş istek sayısı" yok; altyapı var: `slo_latency_mv.go:69 sloGoodFraction` (t-digest ters CDF) | Yeni metrik `slow_count` = `calls − good(thresholdNs)` spanmetrics_1m + EntrySpanKindsWhere; `metricNeedsSampleFloor` false; ConditionPreview/ThresholdField; kapalı gelen builtin "≥50 giriş isteği 2 sn'den yavaş / 5 dk" | ~yarım gün | Additive |
| S6 | 🟡 (+🔴 alt) | SLI qualifier: route/root-span; success iş-mantığı olabilir; servis sınırında ölç | `SLO{Service, SLIType, Target, ThresholdMs, Operation}` (`slo.go:17-27`) — route/status/iş-boolean yok; latency giriş-span'a daralmış (`slo.go:115`), **availability değil** (`service_summary_5m` kind yok, `slo.go:135-152,468-489`) → DB-client span hataları availability'yi seyreltir/şişirir (783'te error_rate için düzeltilen sınıf) | (a) availability → `spanmetrics_1m + EntrySpanKindsWhere` (783 deseni), MV/raw aynı kapsam testi — SLI sayıları oynar; (b) `SLO.Qualifier{Kind, Routes}` route rollup ailesinden; iş-boolean SLI için /spec | (a) ~1 sa · (b) ~1-2 g | (a) davranış değişikliği → onay · (b) additive |
| V3 | 🟡 | Bulguyu kaydet: kök-neden metni + sorgular + SLO'lar | Incident timeline `note` (`api_incidents.go:176`); problem `AISummary` tek-yazımlık; sohbetler `ai_conversations.go`'da ama probleme/olaya bağlı değil; chat/explain/BubbleUp sonucunu sabitleyen yol yok | `ChatBubble`/`AIDrawerBody` "Olaya sabitle" → `POST /api/incidents/{id}/events` (note + build_link + ref_id); incident'sız problem için `AttachProblemToIncident` ya da `problem_events` (aiops §2) | ~2-3 sa | Additive |
| V4 | 🟡 | Oryantasyon SLO + tetiklerle başlar | `guidedRootCauseBundle` / `guidedServiceHealthBundle` (`copilot_guided.go:2102, 2222`) SLO durumu okumaz; `systemChatAgentLoop` araç listesinde SLO yok; SLO yalnız izole `explain-slo` | Paketlere `slo_status` adımı (ListSLOs + 30 sn cache'li status/forecast, "bütçe %X, tükenme ~Y sa"); `/mcp-tools` `get_slos` | ~2 sa | Additive |
| S7 | 🟢 | 5-10 dk minimum süre, flapping önle | Builtin'ler uyumlu (ForSec/Cooldown/MinSamples, `evaluator.go:295-361`, `stamps.go`); SLO burn'de yok: `slo_burn.go:84-142` ilk tikte açar, eşik altında hemen kapatır → 14.4 sınırında aç/kapat çalkantısı (dedup çoğunu yutar, Problems listesi/incident çalkalanır) | `evaluateSLOBurn`'de `stamps.go` ile ForSec=2 tik, Cooldown=30 dk | ~1 sa | Davranış değişikliği (daha az açılış) → onay |
| V5 | 🟢 | Burn pencereleri tutarlı okunmalı | `copilotExplainSLO` (`api.go:9445`) fast/slow'u 5 dk / 1 sa ölçüyor, evaluator 1 sa/6 sa · 6 sa/24 sa (`slo_burn.go:35-42`); modal (`Slos.tsx:512-513`) etiketsiz | `burnPolicies` pencerelerini `chstore` sabitine çıkar; explain + Slos etiketleri aynı kaynaktan | ~20 dk | Additive **GEMİDE v0.10.794** |

Zaten iyi: baseline-first otomatik SLO (`slo_autocreate.go:34-52,165-193`: 7 gün ölçüm, hedef ölçülen −%0.5, taban %99); latency giriş-span; dual-window burn SRE Workbook değerleri (`slo_burn.go:12-26`); kural adları "ne + eşik + pencere", `AlertRule.RunbookURL` bildirimde; pager-vs-chat ayrımı `{Priority, Kind}` kanal eşleşmesiyle (S1 düzelince SLO warning chat'te kalır); hakem kalkanları (`rca_shields.go`) güveni yalnız düşürür.

## Zaten iyi olan (dört ajan da övdü)

- Otomatik çözüm + çözüm bildirimi + incident kaskadı; 30 dk aynı servis +
  1-hop komşu incident gruplama; AND'li kanal yönlendirme; runbook koşusu
  üç katman audit; postmortem taslağı + KB; maintenance window.
- Robust medyan+MAD, yön-farkındalı politika, asimetrik aç/çöz, ±15 dk
  mevsimsel slot + hafta sonu sınıfı, iki-fazlı kümeleme, Spearman gölge
  kipi, deploy etkisi ölçümlü gerekçe.
- MCP hata zarfı (`isError` + `{error,retryable,hint}`), sunucu-tarafı
  sınırlar (clampLimit, range_s, has_more, 20 s bütçe), çağrı başına audit +
  span, kimlik başına rate limit, iki görünümlü tool açıklaması; Streamable
  yol zaten stateless; sunucu sampling/roots/logging kullanmadığı için
  2026-07-28 deprecation saati Coremetry'yi ilgilendirmiyor.
- Baseline-first otomatik SLO, giriş-span latency SLI, dual-window burn
  (SRE Workbook), kural adı/runbook bağlantısı, hakem kalkanları.

## Sorulacak (davranış değişikliği — onaysız uygulanmaz)

S1 (SLO problemleri yaş merdiveninden muaf), S2 (olaysız SLO "Olay yok"),
A1 (hafta sonu mevsimsel taban), I1 (incident şiddeti yükselir), I5
(postmortem prompt bölümleri), A6(2)(3) (soğuk başlangıç kapısı), A8
(bakımda anomali açılmaz), S6a (availability giriş-span), S7 (burn
hysteresis), M3/M4 (stdio env allowlist, CORS origin allowlist), M6
(tool kataloğu hash), L1 (LLM eşzamanlılık), I14 (Sustur süresi), A11
(anomali önceliği tepe).

## Önerilen ilk dilim (~1 gün, çoğu additive)

1. **V5** burn pencereleri tek kaynak (~20 dk) → **M9** tools/list sıralı +
   bayat yorumlar (~15 dk) → **I12** incident P rozeti (~30 dk) → **I15**
   incident satırında problem sayısı (~30 dk) → **I9a** bildirimde On-call
   bağlantısı (~30 dk).
2. Onay alınınca sıra: **A1** (30 dk + test) → **S1** (30 dk) → **S2**
   (1-2 sa) → **I1** (1 sa) → **M3 + M4** (1 g) — bunlar incident/alarm
   doğruluğunu ve güvenliği düzeltir.
3. İkinci dalga (additive, büyük): **M1 + M2** MCP 2026-07-28 uyumu
   (1.5-2 g), **A2** 3.5σ-6σ olayları (yarım gün), **V1** BubbleUp
   prefetch (yarım gün), **S3 + S4** tükenme alarmı + açıklama (yarım gün),
   **I2** eskalasyon zinciri (yarım gün), **L2** prompt önek sırası +
   `cached_tokens` (1 g).

Uygulanan maddeler bu dosyada "**GEMİDE vX**" olarak işlenir (2026-09-19: ilk dilim 794-798; altı 🔴 799-804 — A1/S1/S2/I1/M3/M4 operatör onayı "Hepsi")
(`project-external-skills-audit-2026-09-19` hafızası izler).
